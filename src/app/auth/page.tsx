import { FC } from 'react';

interface Props {}

const Page: FC<Props> = ({}) => {
  return (
    <div className="flex items-center justify-center pointer-events-none select-none h-[calc(100vh-5rem)]"></div>
  );
};

export default Page;
